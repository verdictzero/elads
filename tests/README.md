# tests

Placeholder for the test suite (wired in Phase 1). Planned coverage:
- Archive round-trip (WAD/PK3 load→save byte-diff).
- Entry-type detection fixtures.
- UDMF parse/serialize round-trip.
- Sector triangulation on pathological polygons.
- Headless EGL offscreen render smoke test (frame-hash compare) for CI.

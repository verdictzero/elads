# cmake/thirdparty.cmake — vendored / discovered third-party dependencies.
#
# Placeholder for Phase 1. Will vendor header-only libs (earcut.hpp, sol2) and
# wire Scintilla/Lexilla, and locate the Doom toolchain built by
# scripts/build-toolchain.sh (ZDBSP, acc, AJBSP) via ELADS_TOOLCHAIN_DIR.
#
# Intentionally empty while ELADS_STUB=ON.
